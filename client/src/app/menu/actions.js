"use server";

export default async function fetchMenu() {
  try {
    // TODO: get req /get-menu endpoint when its implemented
    const testMenu = {
    }
    return testMenu
  } catch (error) {
    throw error;
  }
}